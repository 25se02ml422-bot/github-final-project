# Simple Interest Calculator

A simple Bash script calculator to compute simple interest based on user inputs.

## Formula
Simple Interest = (Principal * Rate * Time) / 100

## Usage
Run `./simple-interest.sh` and enter the principal, rate, and time in years when prompted.
